import React from 'react'

export default function Card() {
  return (
    <>
    <h1>This is form component</h1>
    <p>Classes are used throughout, so your markup can be super flexible. Use  s like above,   if the order of your items is important, or roll your own with a   element. Because the .nav uses display: flex, the nav links behave the same as nav items would, but without the extra markup.</p>
    </>
  )
}
